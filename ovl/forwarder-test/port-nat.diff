diff --git a/ovl/forwarder-test/default/bin/forwarder-test_test b/ovl/forwarder-test/default/bin/forwarder-test_test
index dc22a48..64777a5 100755
--- a/ovl/forwarder-test/default/bin/forwarder-test_test
+++ b/ovl/forwarder-test/default/bin/forwarder-test_test
@@ -497,7 +497,7 @@ check_flow_vips() {
 	local flow=$2
 	$kubectl exec $pod -c load-balancer -- nft -j list ruleset > /tmp/ruleset || tdie
 	cat /tmp/ruleset |\
-		jq ".nftables[].set|select(. != null)|select(.name|match(\".*-$flow\"))" > /tmp/vipsets
+		jq ".nftables[].set|select(. != null)|select(.name|match(\"7777-5001-TCP.*\"))" > /tmp/vipsets
 	local n
 	n=$(cat /tmp/vipsets | jq -s length)
 	test $n -eq 2 || tdie "Found sets $n, expected 2"
diff --git a/ovl/forwarder-test/default/etc/kubernetes/forwarder-test/conf/port-nat-basic.yaml b/ovl/forwarder-test/default/etc/kubernetes/forwarder-test/conf/port-nat-basic.yaml
index 75e3860..b292585 100644
--- a/ovl/forwarder-test/default/etc/kubernetes/forwarder-test/conf/port-nat-basic.yaml
+++ b/ovl/forwarder-test/default/etc/kubernetes/forwarder-test/conf/port-nat-basic.yaml
@@ -10,6 +10,13 @@ data:
     items:
     - name: $CONDUIT1
       trench: $NAME
+      destination-port-nats:
+      - port: 7777
+        target-port: 5001
+        vips:
+        - vip1
+        - vip2
+        protocol: TCP
   streams: |
     items:
     - name: $STREAM1
@@ -25,28 +32,6 @@ data:
       protocols:
         - tcp
       stream: $STREAM1
-      local-port: 5001
-    - name: multi-dport
-      vips:
-        - vip1
-        - vip2
-      destination-port-ranges:
-        - 8888
-        - 9999
-      protocols:
-        - tcp
-      stream: $STREAM1
-      local-port: 5001
-    - name: dport-range
-      vips:
-        - vip1
-        - vip2
-      destination-port-ranges:
-        - 8800-9000
-      protocols:
-        - tcp
-      stream: $STREAM1
-      local-port: 5001
   vips: |
     items:
     - name: vip1
diff --git a/ovl/forwarder-test/default/etc/kubernetes/forwarder-test/conf/port-nat-vip2.yaml b/ovl/forwarder-test/default/etc/kubernetes/forwarder-test/conf/port-nat-vip2.yaml
index 121ba70..2f4dfc3 100644
--- a/ovl/forwarder-test/default/etc/kubernetes/forwarder-test/conf/port-nat-vip2.yaml
+++ b/ovl/forwarder-test/default/etc/kubernetes/forwarder-test/conf/port-nat-vip2.yaml
@@ -10,6 +10,15 @@ data:
     items:
     - name: $CONDUIT1
       trench: $NAME
+      destination-port-nats:
+      - port: 7777
+        target-port: 5001
+        vips:
+        - vip1
+        - vip2
+        - vip3
+        - vip4
+        protocol: TCP
   streams: |
     items:
     - name: $STREAM1
@@ -27,7 +36,6 @@ data:
       protocols:
         - tcp
       stream: $STREAM1
-      local-port: 5001
   vips: |
     items:
     - name: vip1
diff --git a/ovl/forwarder-test/forwarder-test.sh b/ovl/forwarder-test/forwarder-test.sh
index d549ed8..3500cfd 100755
--- a/ovl/forwarder-test/forwarder-test.sh
+++ b/ovl/forwarder-test/forwarder-test.sh
@@ -472,7 +472,7 @@ test_port_nat_basic() {
 	otc 1 "configmap $trench conf/port-nat-basic"
 	otc 1 "check_flow $trench port-nat"
 	tcase "Dealy 5s ..."; sleep 5
-	otc 1 "negative_check_flow $trench flow1 multi-dport dport-range"
+	otc 1 "negative_check_flow $trench flow1"
 	otc 202 "mconnect_adr 10.0.0.1:7777"
 	otc 202 "mconnect_adr [1000::1:10.0.0.1]:7777"
 	xcluster_stop
@@ -490,7 +490,7 @@ test_port_nat_vip() {
 	local trench=red
 	trench_test $trench
 	__port=7777
-	otc 1 "configmap $trench conf/port-nat-vip1"
+	otc 1 "configmap $trench conf/port-nat-basic"
 	otc 1 "check_flow $trench port-nat"
 	otc 202 "mconnect_adr 10.0.0.1:7777"
 	otc 202 "mconnect_adr [1000::1:10.0.0.1]:7777"
@@ -499,7 +499,7 @@ test_port_nat_vip() {
 	mconnect_trench $trench
 	otc 1 "check_flow_vips --cnt=4 $trench"
 	test "$__no_stop" = "yes" && exit 0
-	otc 1 "configmap $trench conf/port-nat-vip1"
+	otc 1 "configmap $trench conf/port-nat-basic"
 	otc 202 "mconnect_adr 10.0.0.1:7777"
 	otc 202 "mconnect_adr [1000::1:10.0.0.1]:7777"
 	otc 1 "check_flow_vips --cnt=2 $trench"
